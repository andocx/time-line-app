import ProgressBar from "../../components/ProgressBar";

const SidebarRight = () => {
    return (
        <div className="w-full h-full flex items-center justify-center"> {/* Ensure full width */}
            <ProgressBar />
        </div>
    );
};

export default SidebarRight;